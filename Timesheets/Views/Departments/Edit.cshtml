@model Timesheets.Models.Department

@{
    ViewBag.title = "Edit";
}

<h1>Edit</h1>

<h4>Department</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="DepartmentHead" class="control-label"></label>
                <select asp-for="DepartmentHeadId" class="form-control" asp-items=@ViewBag.managers></select>
                <span asp-validation-for="DepartmentHeadId" class="text-danger"></span>
            </div>




            
            <!--   <label asp-for="RelatedProject" class="control-label"></label>
            <select asp-for="RelatedProject" class="form-control" asp-items=projects>
                <option value="">Please select Project...</option>
            </select>
            <span asp-validation-for="RelatedProject" class="text-danger"></span>

            <select asp-for=DepartmentHead class="form-control" asp-items=>
            -->

            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>
@if (ViewBag.error!=null)
{
 

    <p class="@ViewBag.alertClass" role="alert">@ViewBag.message</p>
}

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
